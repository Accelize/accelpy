---
apiVersion: v1
kind: Service
metadata:
  name: kubernetes-node-test
  labels:
    name: kubernetes-node-test
spec:
  type: NodePort
  selector:
    app: kubernetes-node-test
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
